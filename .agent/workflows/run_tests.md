---
description: Running Unit Tests
---
To run the unit tests for the application, use the following command:

```bash
npm test
```

This will execute Jest and run all test files located in `__tests__` directories.
